const createMessage = require("./createMessage");
const getMessages = require("./getM");
const deleteMessage = require("./deleteMessage");

module.exports = { createMessage, getMessages, deleteMessage };
