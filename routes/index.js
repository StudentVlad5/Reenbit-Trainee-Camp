const routerAuth = require("./auth");
const routerUsers = require("./users");
const routerMessages = require("./message");

module.exports = {
  routerAuth,
  routerUsers,
  routerMessages
};
